# cnn-lib - docker

Two dockerfiles are provided in this directory. The choice should depend on
your hardware options:

* `docker-cpu` - if you plan to run your docker on CPUs (what a surprise)
* `docker-gpu` - if you plan to run your docker on GPUs (surprise, surprise)
